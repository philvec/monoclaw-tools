# monoclaw-tools

MCP Server with common additional tools for monoclaw assistant

**codebase:** 165 lines

## Tools

- **web** (`tools/web.py`)
  - `web_search` — Search the web and return a summary of results.
  - `web_fetch` — Fetch a URL and return its content as markdown. 
